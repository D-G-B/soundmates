
<div class="message-container" id="message-<%= message.id %>">
  
  <% if message.user == current_user %>
  <div class="container d-flex flex-row-reverse"> 
  <% else %> 
  <div class="container"> 
  <% end %>

    <% if message.user == current_user %>
    <div class="card-body card text-white bg-dark mb-3" style="max-width: 18rem;" id="color-current-user">
    <% else %>
    <div class="card-body card text-white bg-dark mb-3" style="max-width: 18rem;">
    <% end %>
      <blockquote class="blockquote mb-0">
        <p id="message-<%= message.id %>"><%= message.content %></p>
          <footer class="blockquote-footer">
            <cite title="Source Title">
              <i class="author">
                <span><%= message.user.username %></span>
                <small><%= message.created_at.strftime("%a %b %e at %l:%M%p") %></small>
              </i>
            </cite>
          </footer>
      </blockquote>
    </div>
  </div> 
</div>


